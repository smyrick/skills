{
  "name": "smyrick-skills",
  "private": true,
  "license": "MIT",
  "description": "Personal agent skills library for Shane Myrick",
  "scripts": {
    "add-skill": "cd skills && npx skills init",
    "format": "node scripts/format-skills.mjs",
    "format:check": "node scripts/format-skills.mjs --check",
    "check": "npm run format:check && npm run validate",
    "validate": "node scripts/validate-skills.mjs"
  },
  "devDependencies": {
    "yaml": "^2.8.3"
  }
}
